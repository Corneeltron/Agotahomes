const Session = require('../models/sessionsModel');
const sessionsController = {};


sessionsController.IsLoggedIn = (req, res, next) => {
  //add stuff
}

sessionsController.startSession = (req, res, next) => {
  //add stuff
}

module.exports = sessionsController; 
